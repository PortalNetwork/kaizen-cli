# Kaizen-cli

- kaizen-cli Install:
```
npm i kaizen-cli -g
```


- Create a project:
```
kaizen dapp -n < package name > -t < vue(default) or react >'

// kaizen dapp -n test -t vue  =>  < vue-test >
```